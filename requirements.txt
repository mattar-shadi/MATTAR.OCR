# MATTAR.OCR – Python dependencies for the Hugging Face OCR engine
#
# These packages are only required when using HuggingFaceOcrService /
# OcrEngine.HuggingFace.  The Tesseract engine (default) has no Python
# prerequisites.
#
# Install with:
#   pip install -r requirements.txt
#
# For GPU acceleration (optional but recommended for large documents):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

transformers>=4.30.0
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0

# Optional – speeds up model loading and inference on supported hardware
# accelerate>=0.20.0
